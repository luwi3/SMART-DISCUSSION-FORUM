; Default upload_max_filesize/post_max_size are far smaller than the 20MB
; Laravel's own validation allows (ResourceController: max:20480) — PHP was
; silently dropping larger files before they ever reached Laravel.
upload_max_filesize = 25M
post_max_size = 30M
